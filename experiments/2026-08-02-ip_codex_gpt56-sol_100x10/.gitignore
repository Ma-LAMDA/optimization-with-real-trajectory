# Local executable copies and dependency links are runtime scaffolding, not data.
/runtime/
/runtime/codex.exe
/runtime/codex-windows-sandbox-setup.exe
/runtime/openai-docs-cache/
/runtime/spreadsheet_artifact/
/runtime/workspace/.git/
/scripts/node_modules/

# The 0802 generated questions, trajectories, reports, and logs stay local.
/results/

# Transient lock files are runtime state, not trajectory data.
/results/runs/**/*.lock
